# web-sacraping-psychologytoday

# How to run this scrapper?

Clone the git repo

Navigate to the directory and type `npm i` from the terminal to download all the dependencies.

Then again type `node app.js` to run the file.
